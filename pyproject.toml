[project]
name = "osint-research-platform"
version = "0.1.0"
description = "Professional OSINT Research Platform"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
